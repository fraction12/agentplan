# @agentplan/core (Scaffold)

This package will host the core planning domain model, orchestration logic, and shared interfaces.

Current state:
- Repository split scaffold only.
- Existing Python package and CLI continue to run from the root package unchanged.
