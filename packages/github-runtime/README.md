# @agentplan/github-runtime (Scaffold)

This package will host GitHub-specific runtime integration points (Actions and CI-oriented execution).

Current state:
- Repository split scaffold only.
- Existing root package remains the active runtime in this phase.
