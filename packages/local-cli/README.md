# @agentplan/local-cli (Scaffold)

This package will host local developer CLI entrypoints and UX for interactive workflows.

Current state:
- Repository split scaffold only.
- Existing `agentplan` CLI behavior remains unchanged in this phase.
